#include "main.h"
#include <string.h>

/**
 * _strcmp - compare two strings pointers
 * @s1: first string param
 * @s2: second string param
 * Return: integer
 */
int _strcmp(char *s1, char *s2)
{
	int result;

	result = strcmp(s1, s2);
	return (result);
}
